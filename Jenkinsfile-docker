pipeline {
    // agent {
    //     label 'maven'
    // }

    agent {
            docker 'maven:alpine-test'
            label 'docker'
    }

    stages {
        stage('Checkout') {
            steps{
                checkout scm
            }

        }
        stage('Build') {
            steps{
                sh "mvn clean install"
            }
        }
    }
}
