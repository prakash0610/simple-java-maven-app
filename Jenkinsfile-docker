pipeline {
    // agent {
    //     label 'maven'
    // }

    agent {
        docker {
            label 'docker'
            image 'maven:alpine-test'
        }
    }

    stages {
        stage('Checkout') {
            steps{
                checkout scm
            }

        }
        stage('Build') {
            steps{
                sh "mvn clean install"
            }
        }
    }
}
