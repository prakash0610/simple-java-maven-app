pipeline {
  agent {
    label 'agent1'
  }

  stages {
    stage ('Checkout') {
      steps {
        sheckout scm
      }
    }
    stage('Build') {
      steps {
        sh "mvn clean install"
      }
    }
  }
}
